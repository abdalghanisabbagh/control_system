import 'package:flutter/material.dart';

class EditSchoolDataWidget extends StatelessWidget {
  const EditSchoolDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
