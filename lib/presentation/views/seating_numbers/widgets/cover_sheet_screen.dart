import 'package:flutter/material.dart';

class CoverSheetsScreen extends StatelessWidget {
  const CoverSheetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
