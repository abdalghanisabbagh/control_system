import 'package:flutter/material.dart';

class EditProctor extends StatelessWidget {
  const EditProctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
